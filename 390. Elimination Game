class Solution {
    public int lastRemaining(int n) {
        if(n == 1){
            return 1;
        }
        return 2 * (n/2 - lastRemaining(n / 2) + 1);
    }
}


//solution 2 need to be fulled, iteratally, O(log n)
